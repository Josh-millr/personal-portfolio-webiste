import LocalFont from "next/font/local";

export const circular = LocalFont({
	src: "./CircularStd-Book.woff2",
	variable: "--font-circular",
});

export const power = LocalFont({
	src: "./PowerGrotesk-Regular.woff2",
	variable: "--font-power",
});
